# Swift / SwiftPM
.build/
.swiftpm/
# Package.resolved is committed (this is an app) to pin dependency versions.

# Xcode userdata (if anyone opens the package in Xcode)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Built app bundle (rebuilt by make-app.sh on demand)
BigEdit.app/

# Release artifacts (built by scripts/make-dmg.sh and CI)
*.dmg

# macOS
.DS_Store
